version: "5.0"
services:
  db:
    container_name: "db"
    image: postgres:16.0-alpine
    restart: always
    env_file:
      - "./app/.env"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: